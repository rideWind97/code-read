装饰器是 TypeScript 和现代 JavaScript（处于提案阶段）中一种非常强大的元编程特性，它允许你在类、方法、属性、参数等声明时，通过“装饰”的方式动态地修改或增强其行为。

---

## 一、装饰器的基本语法

装饰器本质上是一个函数，它在**代码定义时**（而非运行时）被调用，接收目标对象和上下文信息，并可返回新的定义来“修改”原对象。

### 1. 装饰器的几种类型

| 类型 | 示例 |
|------|------|
| 类装饰器 | `@sealed class MyClass` |
| 方法装饰器 | `@logMethod public getName()` |
| 属性装饰器 | `@observable name` |
| 参数装饰器 | `constructor(@inject('logger') logger)` |
| 访问器装饰器 | `@readonly get value()` |

---

## 二、装饰器的原理

装饰器的原理基于 **JavaScript 的元数据反射（Reflection）机制** 和 **函数劫持 / 包装**。

### 1. 装饰器函数的调用时机

装饰器在**类定义时执行**，而不是实例化时。也就是说：

```ts
@decorator
class A {}
// 此时 decorator 函数已经执行了
```

### 2. 装饰器函数的参数

不同类型的装饰器接收不同的参数：

#### ✅ 类装饰器
```ts
function classDecorator(constructor: Function) {
  // 修改类的构造函数或原型
}
```

#### ✅ 方法装饰器
```ts
function methodDecorator(
  target: any,           // 类的原型（实例方法）或构造函数（静态方法）
  propertyKey: string,   // 方法名
  descriptor: PropertyDescriptor  // 属性描述符
) {
  // 可以修改 descriptor.value（原函数）
}
```

#### ✅ 属性装饰器
```ts
function propertyDecorator(
  target: any,
  propertyKey: string
) {
  // 注意：属性装饰器不能修改属性本身（因为属性还没初始化）
  // 通常用于记录元数据
}
```

#### ✅ 参数装饰器
```ts
function paramDecorator(
  target: any,
  propertyKey: string | undefined,
  parameterIndex: number
) {
  // 记录某个参数需要什么依赖
}
```

---

## 三、手写几个实用的装饰器示例

### 示例 1：@readonly（使方法只读）

```ts
function readonly(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  descriptor.writable = false; // 不允许重写
}

class Person {
  name = "Alice";

  @readonly
  greet() {
    return "Hello " + this.name;
  }
}

const p = new Person();
// p.greet = () => "hacked"; // ❌ 会报错（在 strict 模式下）
```

---

### 示例 2：@log（日志装饰器）

```ts
function log(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const originalMethod = descriptor.value;

  descriptor.value = function (...args: any[]) {
    console.log(`Calling "${propertyKey}" with arguments:`, args);
    const result = originalMethod.apply(this, args);
    console.log(`Result:`, result);
    return result;
  };

  return descriptor;
}

class Calculator {
  @log
  add(a: number, b: number) {
    return a + b;
  }
}

new Calculator().add(2, 3);
// 输出：
// Calling "add" with arguments: [2, 3]
// Result: 5
```

---

### 示例 3：@debounce（防抖装饰器）

```ts
function debounce(delay: number) {
  return function (
    target: any,
    propertyKey: string,
    descriptor: PropertyDescriptor
  ) {
    let timer: any;
    const originalMethod = descriptor.value;

    descriptor.value = function (...args: any[]) {
      clearTimeout(timer);
      timer = setTimeout(() => {
        originalMethod.apply(this, args);
      }, delay);
    };

    return descriptor;
  };
}

class Button {
  @debounce(500)
  onClick() {
    console.log("Button clicked");
  }
}
```

---

### 示例 4：@inject（依赖注入装饰器）

```ts
// 简化版，配合容器使用
const registry: { [key: string]: any } = {};

export function register(token: string, useValue: any) {
  registry[token] = useValue;
}

function inject(token: string) {
  return function (
    target: any,
    propertyKey: string | undefined,
    parameterIndex: number
  ) {
    // 这里可以结合参数装饰器，记录“第几个参数需要什么 token”
    Reflect.defineMetadata(
      `design:dependency:${parameterIndex}`,
      token,
      target,
      propertyKey || 'constructor'
    );
  };
}

// 使用时由容器解析元数据并注入
```

> 配合 `Reflect Metadata` 可以实现类似 Angular 的依赖注入。

---

## 四、装饰器背后的原理详解

### 1. 装饰器 = 高阶函数 + 属性描述符操作

装饰器通过修改 `PropertyDescriptor`（`value`, `writable`, `get`, `set` 等）来改变方法或属性的行为。

### 2. 元数据反射（Reflect Metadata）

TypeScript 支持通过 `reflect-metadata` 库保存和读取装饰器的元数据：

```ts
import "reflect-metadata";

// 保存元数据
Reflect.defineMetadata('role', 'admin', User, 'login');

// 读取元数据
const role = Reflect.getMetadata('role', User, 'login');
```

这在依赖注入、路由装饰器（如 `@Get('/users')`）中非常关键。

### 3. 编译器支持

你需要在 `tsconfig.json` 中启用：

```json
{
  "experimentalDecorators": true,
  "emitDecoratorMetadata": true
}
```

---

## 五、装饰器的局限性

| 限制 | 说明 |
|------|------|
| 不能用于函数表达式 | 只能用于类、方法、属性等声明 |
| 属性装饰器不能修改属性值 | 只能记录元数据 |
| 浏览器原生支持有限 | 需要编译（Babel / TypeScript） |
| 性能开销 | 多一层函数包装 |

---

## 六、实际应用场景

| 场景 | 装饰器示例 |
|------|-----------|
| 权限控制 | `@roles('admin')` |
| 路由映射 | `@Get('/users')` |
| 数据校验 | `@validate()` |
| 日志监控 | `@log()` |
| 缓存 | `@cache(60)` |
| 依赖注入 | `@inject('logger')` |

---

## 七、总结

### 装饰器的核心原理：

1. **装饰器是一个函数**，在声明时自动执行。
2. **通过修改 `PropertyDescriptor`** 来改变方法行为（如 `writable`, `value`）。
3. **利用 `Reflect Metadata`** 存储额外信息，供运行时使用。
4. **依赖编译器支持**（TS 或 Babel）。

### 手写装饰器的关键步骤：

1. 确定装饰器类型（类、方法、属性等）
2. 接收正确的参数（target, key, descriptor）
3. 修改行为（包装函数、设置元数据等）
4. 返回新的 descriptor（方法装饰器）

---
